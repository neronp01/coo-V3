import { NameDirectiveDirective } from './name-directive.directive';

describe('NameDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NameDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
