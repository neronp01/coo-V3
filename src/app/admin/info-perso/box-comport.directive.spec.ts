import { BoxComportDirective } from './box-comport.directive';

describe('BoxComportDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxComportDirective();
    expect(directive).toBeTruthy();
  });
});
