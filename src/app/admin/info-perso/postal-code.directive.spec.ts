import { PostalCodeDirective } from './postal-code.directive';

describe('PostalCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new PostalCodeDirective();
    expect(directive).toBeTruthy();
  });
});
